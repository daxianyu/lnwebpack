/**
 * 全局变量
 */
/* global module */

let appConfig = {
        SECURITY_TOKEN_NAME: 'sso-security-ticket',
    }, tokenCookie = 'sso-security-ticket';

module.exports = {
    appConfig, tokenCookie ,
};
